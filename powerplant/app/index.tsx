import { Text, View } from "react-native";

export default function Index() {
  return (
    <View className="flex-1 items-center justify-center bg-night">
      <Text className="text-primary-soft text-2xl font-bold">Powerplan(t)</Text>
    </View>
  );
}
